# 本地路径说明

**2026-05-09**：本 **`融资/`** 目录已调整为 **投资人自包含材料包**——正文 **不依赖** 内部 `product-management-office/` 等幕僚仓库路径；维护时以 **本目录各骨架 + VDR 原件** 为准。

## 可选：执行稿草稿与主仓库

若你仍将 **One Pager / FAQ / Deck 页序** 放在与融资包并列的 **`TT-Expedition-docs`** 仓库中，可在编辑器内保留或替换以下前缀（仅影响 **`01`** 中可选链接）：

```text
../TT-Expedition-docs/
```

表示：**本 `融资` 文件夹的上一级目录下的 `TT-Expedition-docs` 文件夹**。

若无可选仓库，删除或忽略 **`01`** 中指向 `product-manager/` 的链接即可，**不影响** 对本包其余文件的阅读。

**不要**把含真实用户数据、密钥、律所保密附件的路径写进公开 Git 仓库正文；可写「Off-repo：VDR 路径 + Owner」。
