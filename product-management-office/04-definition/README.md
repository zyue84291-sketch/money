# 04-definition — 需求定义

**目标**：PRD 与需求池 **可追溯到 `spec`**，并与工程 Epic / 路由变更门禁一致。

## 本目录文件

| 文件 | 说明 |
|------|------|
| `00-PRD-最小结构模板.md` | 企业级 PRD 骨架（含 **v1 冻结 / §0.6 改动记录**、spec 锚点区） |
| `01-需求池与Epic对齐-spec-入口.md` | 需求池字段 + **PriorityScore（Impact×Confidence÷Effort）** + 回链 `spec/07`、`53`、`04` |

## 工程门禁提醒

- 新增 **`/api/v1/...`** 公共路由：必须先更新 **`spec/04` §3.4** 机读窗（见 `spec/00` 读前 B-181 说明）。
